[package]
name = "rocksdb-tui-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building rocksdb-tui WASM plugins"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
serde_json = "1"

[dev-dependencies]
anyhow = "1"
