[package]
name = "fiber-types"
version = "0.1.0"
edition = "2021"
description = "Fiber Network data types for rocksdb-tui plugin example"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1"
